[project]
name = "soroban-scan-api"
version = "0.1.0"
description = "x402-paywalled HTTP API for soroban-scan"
requires-python = ">=3.11"
dependencies = [
    "fastapi[standard]>=0.115",
    "httpx>=0.27",
    "pydantic>=2.0",
]
